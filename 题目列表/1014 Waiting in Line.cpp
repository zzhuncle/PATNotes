#include<bits/stdc++.h>
using namespaces std;
int main()
{
    int n, m, k, q;
    cin >> n >> m >> k >> q;
    vector<int> process_t(k);
    for (int i = 0;i < k;i++)
        cin >> process_t[i];
    int query;
    for (int i = 0;i < q;i++) {
        cin >> query;
        
    }
    return 0;
}